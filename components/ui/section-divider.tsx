// Design-system SectionDivider (docs/design-system.md §3) — the hairline
// `.sec-divider` rule between page sections (12px-rhythm spacing in CSS).
export function SectionDivider() {
  return <hr className="sec-divider" />;
}
